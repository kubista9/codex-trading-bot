# Dashboard

React dashboard scaffold placeholder.

Planned views:

- model overview
- prediction outputs
- signal dashboard
- feature importance charts
- backtest performance charts
- dataset coverage and missingness summaries
- experiment tracking summaries

The first implementation should consume saved artifacts. Add a FastAPI layer only when artifact-only loading becomes limiting.
